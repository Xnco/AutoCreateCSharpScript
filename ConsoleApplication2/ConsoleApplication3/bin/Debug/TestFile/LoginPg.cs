//------------------------------------------------------------------------------
// <auto-generated>
//     此代码由工具生成。
//     运行时版本:4.0.30319.42000
//
//     对此文件的更改可能会导致不正确的行为，并且如果
//     重新生成代码，这些更改将会丢失。
// </auto-generated>
//------------------------------------------------------------------------------

using System;
using System.Collections.Generic;



public class LoginPg : PackageBase
{
    
    public short ID = 1;
    
    public string account			;
    
    public string passwd;
    
    public float version;
    
    public override byte[] Create()
    {
        buf.Add((byte)(ID & (short)filter));
        buf.Add((byte)((ID>> 8) & (short)filter));

        int len = 0;
        byte[] account			ByteArray = Encoding.UTF8.GetBytes(account			);
        byte[] passwdByteArray = Encoding.UTF8.GetBytes(passwd);
        len +=  2 + account			ByteArray.Lenght + 2 + passwdByteArray.Lenght + 4 ;

        buf.Add((byte)(len & (short)filter));
        buf.Add((byte)((len >> 8) & (short)filter));

        buf.Add((byte)(account			ByteArray.Length & (short)filter));
        buf.Add((byte)((account			ByteArray.Length >> 8) & (short)filter));
		for (int i = 0; i < account			ByteArray.Length; i++) 
		{
           buf.Add(account			ByteArray[i]);
		}

        buf.Add((byte)(passwdByteArray.Length & (short)filter));
        buf.Add((byte)((passwdByteArray.Length >> 8) & (short)filter));
		for (int i = 0; i < passwdByteArray.Length; i++) 
		{
           buf.Add(passwdByteArray[i]);
		}

        byte[] byversion = BitConverter.GetBytes(version);
		for (int m = 0; m < byversion.Length; m++) 
		{
           buf.Add(byversion[m]);
		}

        return buf.ToArray();
    }
}
