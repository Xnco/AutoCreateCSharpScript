//------------------------------------------------------------------------------
// <auto-generated>
//     此代码由工具生成。
//     运行时版本:4.0.30319.42000
//
//     对此文件的更改可能会导致不正确的行为，并且如果
//     重新生成代码，这些更改将会丢失。
// </auto-generated>
//------------------------------------------------------------------------------

using System;
using System.Collections.Generic;



public class chat : PackageBase
{
    
    public short ID = 53;
    
    public int desCharid;
    
    public short channel;
    
    public string content;
    
    public System.Collections.Generic.List<float> talkList;
    
    public override byte[] Create()
    {
        buf.Add((byte)(ID & (short)filter));
        buf.Add((byte)((ID>> 8) & (short)filter));

        int len = 0;
        byte[] contentByteArray = Encoding.UTF8.GetBytes(content);
        len += 4 + 2 + 2 + contentByteArray.Lenght + 2 + talkList.Count * 4 ;

        buf.Add((byte)(len & (short)filter));
        buf.Add((byte)((len >> 8) & (short)filter));

        buf.Add((byte)(desCharid & (short)filter));
        buf.Add((byte)((desCharid >> 8) & (short)filter));
        buf.Add((byte)((desCharid >> 16) & (short)filter));
        buf.Add((byte)((desCharid >> 24) & (short)filter));

        buf.Add((byte)(channel & (short)filter));
        buf.Add((byte)((channel >> 8) & (short)filter));

        buf.Add((byte)(contentByteArray.Length & (short)filter));
        buf.Add((byte)((contentByteArray.Length >> 8) & (short)filter));
		for (int i = 0; i < contentByteArray.Length; i++) 
		{
           buf.Add(contentByteArray[i]);
		}

        buf.Add((byte)(talkList.Count & (short)filter));
        buf.Add((byte)((talkList.Count >> 8) & (short)filter));
		for (int i = 0; i < talkList.Count; i++) 
		{
        byte[] bytalkList = BitConverter.GetBytes(talkList[i]);
		for (int m = 0; m < bytalkList.Length; m++) 
		{
           buf.Add(bytalkList[m]);
		}
		}

        return buf.ToArray();
    }
}
